# Claude Code Remote Configuration
# Copy this file to .env and configure with your actual values
# 复制此文件为 .env 并配置你的实际值

# ===== 選擇通知方式：Telegram (推荐) / Notification Method: Telegram (Recommended) =====
TELEGRAM_ENABLED=true

# ===== 其他通知方式 / Other Notification Methods =====
EMAIL_ENABLED=false
LINE_ENABLED=false
DESKTOP_ENABLED=false

# ===== Telegram 配置 / Telegram Configuration =====
# 從 @BotFather 獲取 Bot Token / Get Bot Token from @BotFather
# 步骤：1. 在 Telegram 中搜索 @BotFather
#       2. 发送 /newbot 创建新 bot
#       3. 复制获得的 token
TELEGRAM_BOT_TOKEN=

# Telegram 接收者配置 / Telegram Chat Configuration
# 获取你的 Chat ID: 在 Telegram 中搜索 @userinfobot，发送任意消息获取你的 ID
TELEGRAM_CHAT_ID=

# 群組 ID（可选）/ Group ID (optional)
# 如果要发送到群组，使用群组 ID（通常是負數）
# TELEGRAM_GROUP_ID=

# Telegram 白名單（可选）/ Telegram Whitelist (optional)
# 逗號分隔的 Chat ID，限制谁可以使用 bot
# TELEGRAM_WHITELIST=

# Telegram webhook 配置 / Telegram Webhook Configuration
TELEGRAM_WEBHOOK_PORT=3001

# Telegram webhook URL / Public HTTPS URL for webhook
# 使用 ngrok 或其他内网穿透工具获取公网 URL
# 例如: https://abc123.ngrok-free.app
TELEGRAM_WEBHOOK_URL=

# 強制使用 IPv4（可选）/ Force IPv4 (optional)
# 在某些网络环境下 IPv6 可能不稳定
# TELEGRAM_FORCE_IPV4=false

# ===== 系统配置 / System Configuration =====
# 会话映射文件路径 / Session map path
SESSION_MAP_PATH=/tmp/claude-remote-sessions

# 运行模式 / Injection mode: pty or tmux
INJECTION_MODE=tmux

# Claude CLI 路径 / Claude CLI path
CLAUDE_CLI_PATH=claude

# 日志级别 / Log level: debug, info, warn, error
LOG_LEVEL=info

# 是否记录PTY输出 / Log PTY output (debug)
PTY_OUTPUT_LOG=false

# ===== 超时配置 / Timeout Configuration =====
COMMAND_TIMEOUT=10000
NOTIFICATION_TIMEOUT=3000
NOTIFICATION_DISPLAY_TIME=10000
CHECK_INTERVAL=20
SESSION_TIMEOUT=24

# ===== 其他平台配置 / Other Platform Configurations =====
# (已禁用，如需使用请参考完整文档 / Disabled, see full docs if needed)
# Email, LINE 等其他平台的配置已省略
# 如需使用请查看项目 README 或联系支持