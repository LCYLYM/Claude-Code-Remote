{
  "name": "claude-code-remote-web",
  "version": "2.0.0",
  "description": "Claude Code Remote - Web UI Interface for controlling Claude Code through a beautiful chat interface",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm run start --workspace=server",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "clean": "rm -rf node_modules server/node_modules client/node_modules shared/node_modules server/dist client/dist"
  },
  "keywords": [
    "claude-code",
    "web-ui",
    "chat-interface",
    "terminal",
    "remote-control",
    "websocket",
    "real-time"
  ],
  "author": "Claude-Code-Remote Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
